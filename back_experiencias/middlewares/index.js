const notFound = require("./notFound");
const validateAuth = require("./validateAuth");
const isAdmin = require("./isAdmin");
const errors = require("./errors");

module.exports = { notFound, validateAuth, isAdmin, errors };
